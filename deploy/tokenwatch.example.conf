# Nginx reverse proxy example for TokenWatch.
# Suggested DNS:
#   tokenwatch.dev      -> this server
#   www.tokenwatch.dev  -> this server
#   api.tokenwatch.dev  -> this server (optional clean proxy hostname)

server {
    listen 80;
    server_name tokenwatch.dev www.tokenwatch.dev api.tokenwatch.dev;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name tokenwatch.dev www.tokenwatch.dev api.tokenwatch.dev;

    # Fill these with certbot/Let's Encrypt paths after issuing certs.
    ssl_certificate /etc/letsencrypt/live/tokenwatch.dev/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/tokenwatch.dev/privkey.pem;

    client_max_body_size 10m;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
